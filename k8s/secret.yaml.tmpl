apiVersion: v1
kind: Secret
metadata:
  name: tfds-dashboard-secrets
  namespace: tfds-dashboard
  labels:
    app.kubernetes.io/name: tfds-dashboard
    app.kubernetes.io/component: frontend
type: Opaque
stringData:
  # This template is auto-populated from .env using envsubst
  # Set these values in your .env file (see .env.example)

  # InfluxDB Configuration
  INFLUXDB_TOKEN: "${INFLUXDB_TOKEN}"
